"if (url) return `${m.content || ''}\n${url}`.trim();\n            return m.content || '';\n        }).join('\n---\n');\n\n        if (navigator.clipboard && navigator.clipboard.writeText) {\n            navig"